angular.module('routerApp',['ui.router'])
//     .config(["$locationProvider", function($locationProvider) {
//     $locationProvider.html5Mode(true);
//     // this removes the # from the URL, which is there by default
//     // you should just chain this to your routes. It's here just for example.
// }]);
